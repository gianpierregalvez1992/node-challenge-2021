import VideoController from "../src/Controllers/Video";

describe("VIDEO CRUD Methods", () => {
	it("testing", () => {
		// done();
		const request = {
			body: {},
		};

		const response = {
			status: function () {},
		};
		expect(2).toBe(2);
	});
});
